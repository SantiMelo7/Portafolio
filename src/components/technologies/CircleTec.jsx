export default function CircleTec({ color }) {
    return (
        <div className="p-[0, 4px]">
            <span className={`${color} inline-block items-center w-[10px] h-[10px] p-[1px] rounded-[50%]`}/>
        </div>
    )
}